// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/looplj/axonhub/internal/ent/relaydailyusagesummary"
	"github.com/looplj/axonhub/internal/ent/relaykey"
)

// RelayDailyUsageSummary is the model entity for the RelayDailyUsageSummary schema.
type RelayDailyUsageSummary struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Relay key summarized by this daily aggregate
	RelayKeyID int `json:"relay_key_id,omitempty"`
	// Project scope copied from the owning Relay key for privacy filters
	ProjectID int `json:"project_id,omitempty"`
	// UTC date bucket for the aggregate
	StatDate time.Time `json:"stat_date,omitempty"`
	// Number of settled requests in this day
	RequestCount int64 `json:"request_count,omitempty"`
	// Total settled tokens in this day
	TotalTokens int64 `json:"total_tokens,omitempty"`
	// Downstream charge total stored as a decimal string
	TotalCharge string `json:"total_charge,omitempty"`
	// Upstream cost total stored as a decimal string
	TotalUpstreamCost string `json:"total_upstream_cost,omitempty"`
	// Most recent request included in this aggregate
	LastRequestID *int `json:"last_request_id,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the RelayDailyUsageSummaryQuery when eager-loading is set.
	Edges        RelayDailyUsageSummaryEdges `json:"edges"`
	selectValues sql.SelectValues
}

// RelayDailyUsageSummaryEdges holds the relations/edges for other nodes in the graph.
type RelayDailyUsageSummaryEdges struct {
	// RelayKey holds the value of the relay_key edge.
	RelayKey *RelayKey `json:"relay_key,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
	// totalCount holds the count of the edges above.
	totalCount [1]map[string]int
}

// RelayKeyOrErr returns the RelayKey value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e RelayDailyUsageSummaryEdges) RelayKeyOrErr() (*RelayKey, error) {
	if e.RelayKey != nil {
		return e.RelayKey, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: relaykey.Label}
	}
	return nil, &NotLoadedError{edge: "relay_key"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*RelayDailyUsageSummary) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case relaydailyusagesummary.FieldID, relaydailyusagesummary.FieldRelayKeyID, relaydailyusagesummary.FieldProjectID, relaydailyusagesummary.FieldRequestCount, relaydailyusagesummary.FieldTotalTokens, relaydailyusagesummary.FieldLastRequestID:
			values[i] = new(sql.NullInt64)
		case relaydailyusagesummary.FieldTotalCharge, relaydailyusagesummary.FieldTotalUpstreamCost:
			values[i] = new(sql.NullString)
		case relaydailyusagesummary.FieldCreatedAt, relaydailyusagesummary.FieldUpdatedAt, relaydailyusagesummary.FieldStatDate:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the RelayDailyUsageSummary fields.
func (_m *RelayDailyUsageSummary) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case relaydailyusagesummary.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case relaydailyusagesummary.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case relaydailyusagesummary.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		case relaydailyusagesummary.FieldRelayKeyID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field relay_key_id", values[i])
			} else if value.Valid {
				_m.RelayKeyID = int(value.Int64)
			}
		case relaydailyusagesummary.FieldProjectID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field project_id", values[i])
			} else if value.Valid {
				_m.ProjectID = int(value.Int64)
			}
		case relaydailyusagesummary.FieldStatDate:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field stat_date", values[i])
			} else if value.Valid {
				_m.StatDate = value.Time
			}
		case relaydailyusagesummary.FieldRequestCount:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field request_count", values[i])
			} else if value.Valid {
				_m.RequestCount = value.Int64
			}
		case relaydailyusagesummary.FieldTotalTokens:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field total_tokens", values[i])
			} else if value.Valid {
				_m.TotalTokens = value.Int64
			}
		case relaydailyusagesummary.FieldTotalCharge:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field total_charge", values[i])
			} else if value.Valid {
				_m.TotalCharge = value.String
			}
		case relaydailyusagesummary.FieldTotalUpstreamCost:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field total_upstream_cost", values[i])
			} else if value.Valid {
				_m.TotalUpstreamCost = value.String
			}
		case relaydailyusagesummary.FieldLastRequestID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field last_request_id", values[i])
			} else if value.Valid {
				_m.LastRequestID = new(int)
				*_m.LastRequestID = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the RelayDailyUsageSummary.
// This includes values selected through modifiers, order, etc.
func (_m *RelayDailyUsageSummary) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryRelayKey queries the "relay_key" edge of the RelayDailyUsageSummary entity.
func (_m *RelayDailyUsageSummary) QueryRelayKey() *RelayKeyQuery {
	return NewRelayDailyUsageSummaryClient(_m.config).QueryRelayKey(_m)
}

// Update returns a builder for updating this RelayDailyUsageSummary.
// Note that you need to call RelayDailyUsageSummary.Unwrap() before calling this method if this RelayDailyUsageSummary
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *RelayDailyUsageSummary) Update() *RelayDailyUsageSummaryUpdateOne {
	return NewRelayDailyUsageSummaryClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the RelayDailyUsageSummary entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *RelayDailyUsageSummary) Unwrap() *RelayDailyUsageSummary {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: RelayDailyUsageSummary is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *RelayDailyUsageSummary) String() string {
	var builder strings.Builder
	builder.WriteString("RelayDailyUsageSummary(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("relay_key_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.RelayKeyID))
	builder.WriteString(", ")
	builder.WriteString("project_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.ProjectID))
	builder.WriteString(", ")
	builder.WriteString("stat_date=")
	builder.WriteString(_m.StatDate.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("request_count=")
	builder.WriteString(fmt.Sprintf("%v", _m.RequestCount))
	builder.WriteString(", ")
	builder.WriteString("total_tokens=")
	builder.WriteString(fmt.Sprintf("%v", _m.TotalTokens))
	builder.WriteString(", ")
	builder.WriteString("total_charge=")
	builder.WriteString(_m.TotalCharge)
	builder.WriteString(", ")
	builder.WriteString("total_upstream_cost=")
	builder.WriteString(_m.TotalUpstreamCost)
	builder.WriteString(", ")
	if v := _m.LastRequestID; v != nil {
		builder.WriteString("last_request_id=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteByte(')')
	return builder.String()
}

// RelayDailyUsageSummaries is a parsable slice of RelayDailyUsageSummary.
type RelayDailyUsageSummaries []*RelayDailyUsageSummary
